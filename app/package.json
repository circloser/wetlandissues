{
  "name": "wetland-issues-mapper",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute wetland-db --local --file=schema.sql",
    "db:seed:local": "wrangler d1 execute wetland-db --local --file=seed.sql",
    "test": "node --test test/**/*.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  },
  "dependencies": {
    "leaflet": "^1.9.4",
    "leaflet.markercluster": "^1.5.3"
  }
}
